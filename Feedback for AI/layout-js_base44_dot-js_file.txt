import React, { useState } from "react";
import { Link, useLocation } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { 
  Heart, Home, Map, Church, Calendar, BookOpen, 
  Plus, DollarSign, Menu, X, Search, Phone, Mail
} from "lucide-react";
import {
  Sidebar,
  SidebarContent,
  SidebarGroup,
  SidebarGroupContent,
  SidebarMenu,
  SidebarMenuButton,
  SidebarMenuItem,
  SidebarHeader,
  SidebarProvider,
  SidebarTrigger,
} from "@/components/ui/sidebar";
import { Button } from "@/components/ui/button";

const navigationItems = [
  {
    title: "Home",
    url: createPageUrl("Home"),
    icon: Home,
  },
  {
    title: "Find Resources",
    url: createPageUrl("Directory"),
    icon: Search,
  },
  {
    title: "Interactive Map",
    url: createPageUrl("Map"),
    icon: Map,
  },
  {
    title: "Faith Communities",
    url: createPageUrl("FaithResources"),
    icon: Church,
  },
  {
    title: "Events Calendar",
    url: createPageUrl("Events"),
    icon: Calendar,
  },
  {
    title: "Stories & News",
    url: createPageUrl("Blog"),
    icon: BookOpen,
  },
  {
    title: "Submit Resource",
    url: createPageUrl("SubmitResource"),
    icon: Plus,
  },
  {
    title: "Donate",
    url: createPageUrl("Donate"),
    icon: DollarSign,
  },
];

export default function Layout({ children }) {
  const location = useLocation();
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

  return (
    <div className="min-h-screen bg-gradient-to-b from-blue-50 via-white to-green-50">
      {/* Desktop Header */}
      <header className="hidden lg:block bg-white/95 backdrop-blur-sm border-b border-blue-100 sticky top-0 z-50 shadow-sm">
        <div className="max-w-7xl mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <Link to={createPageUrl("Home")} className="flex items-center gap-3 group">
              <div className="w-12 h-12 bg-gradient-to-br from-blue-500 to-green-500 rounded-xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-shadow">
                <Heart className="w-7 h-7 text-white" />
              </div>
              <div>
                <h1 className="text-xl font-bold text-gray-900">Florida ASD Directory</h1>
                <p className="text-xs text-gray-600">Your guide to neurodivergent-friendly resources</p>
              </div>
            </Link>
            
            <nav className="flex items-center gap-1">
              {navigationItems.map((item) => (
                <Link
                  key={item.title}
                  to={item.url}
                  className={`flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium transition-all ${
                    location.pathname === item.url
                      ? "bg-blue-100 text-blue-700"
                      : "text-gray-700 hover:bg-blue-50"
                  }`}
                >
                  <item.icon className="w-4 h-4" />
                  <span>{item.title}</span>
                </Link>
              ))}
            </nav>
          </div>
        </div>
      </header>

      {/* Mobile Header */}
      <header className="lg:hidden bg-white border-b border-blue-100 sticky top-0 z-50 shadow-sm">
        <div className="px-4 py-3">
          <div className="flex items-center justify-between">
            <Link to={createPageUrl("Home")} className="flex items-center gap-2">
              <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-green-500 rounded-lg flex items-center justify-center">
                <Heart className="w-6 h-6 text-white" />
              </div>
              <div>
                <h1 className="text-lg font-bold text-gray-900">FL ASD Directory</h1>
              </div>
            </Link>
            
            <Button
              variant="ghost"
              size="icon"
              onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
              className="lg:hidden"
            >
              {mobileMenuOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}
            </Button>
          </div>
        </div>

        {/* Mobile Menu */}
        {mobileMenuOpen && (
          <nav className="border-t border-blue-100 bg-white">
            {navigationItems.map((item) => (
              <Link
                key={item.title}
                to={item.url}
                onClick={() => setMobileMenuOpen(false)}
                className={`flex items-center gap-3 px-4 py-3 border-b border-blue-50 ${
                  location.pathname === item.url
                    ? "bg-blue-50 text-blue-700"
                    : "text-gray-700"
                }`}
              >
                <item.icon className="w-5 h-5" />
                <span className="font-medium">{item.title}</span>
              </Link>
            ))}
          </nav>
        )}
      </header>

      {/* Main Content */}
      <main className="min-h-screen">
        {children}
      </main>

      {/* Footer */}
      <footer className="bg-gradient-to-r from-blue-900 to-green-900 text-white mt-20">
        <div className="max-w-7xl mx-auto px-6 py-12">
          <div className="grid md:grid-cols-3 gap-8 mb-8">
            <div>
              <div className="flex items-center gap-3 mb-4">
                <div className="w-10 h-10 bg-white/20 rounded-lg flex items-center justify-center">
                  <Heart className="w-6 h-6" />
                </div>
                <h3 className="text-xl font-bold">Florida ASD Directory</h3>
              </div>
              <p className="text-blue-100 leading-relaxed">
                Connecting Florida's neurodivergent community with resources, support, and understanding.
              </p>
            </div>
            
            <div>
              <h4 className="font-semibold mb-4 text-lg">Quick Links</h4>
              <ul className="space-y-2">
                <li><Link to={createPageUrl("Directory")} className="text-blue-100 hover:text-white transition-colors">Find Resources</Link></li>
                <li><Link to={createPageUrl("Map")} className="text-blue-100 hover:text-white transition-colors">Interactive Map</Link></li>
                <li><Link to={createPageUrl("Events")} className="text-blue-100 hover:text-white transition-colors">Events</Link></li>
                <li><Link to={createPageUrl("SubmitResource")} className="text-blue-100 hover:text-white transition-colors">Submit Resource</Link></li>
              </ul>
            </div>
            
            <div>
              <h4 className="font-semibold mb-4 text-lg">Get Involved</h4>
              <ul className="space-y-2">
                <li><Link to={createPageUrl("Donate")} className="text-blue-100 hover:text-white transition-colors">Support Our Mission</Link></li>
                <li><Link to={createPageUrl("Blog")} className="text-blue-100 hover:text-white transition-colors">Read Stories</Link></li>
              </ul>
              <div className="mt-6 space-y-2">
                <a href="tel:555-0123" className="flex items-center gap-2 text-blue-100 hover:text-white transition-colors">
                  <Phone className="w-4 h-4" />
                  (555) 123-4567
                </a>
                <a href="mailto:info@flasddirectory.org" className="flex items-center gap-2 text-blue-100 hover:text-white transition-colors">
                  <Mail className="w-4 h-4" />
                  info@flasddirectory.org
                </a>
              </div>
            </div>
          </div>
          
          <div className="border-t border-white/20 pt-8 text-center text-blue-100">
            <p>&copy; 2025 Florida ASD Directory. Built with love for the neurodivergent community.</p>
            <p className="mt-2 text-sm">Celebrating neurodiversity across the Sunshine State 🌈</p>
          </div>
        </div>
      </footer>
    </div>
  );
}