import React, { useState } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Badge } from "@/components/ui/badge";
import { 
  Heart, DollarSign, Users, Database, TrendingUp, 
  CheckCircle, CreditCard, Smartphone, Mail
} from "lucide-react";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { Label } from "@/components/ui/label";
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group";

const donationAmounts = [
  { amount: 25, impact: "Helps verify 10 resources" },
  { amount: 50, impact: "Updates 25 listings" },
  { amount: 100, impact: "Adds new features" },
  { amount: 250, impact: "Supports monthly operations" },
];

export default function Donate() {
  const [donationType, setDonationType] = useState("one-time");
  const [amount, setAmount] = useState("50");
  const [customAmount, setCustomAmount] = useState("");
  const [donorInfo, setDonorInfo] = useState({
    name: "",
    email: "",
    message: "",
    anonymous: false,
  });

  const selectedAmount = customAmount || amount;

  return (
    <div className="min-h-screen bg-gradient-to-b from-blue-50 to-white pb-12">
      {/* Hero Section */}
      <div className="bg-gradient-to-r from-blue-600 via-purple-600 to-green-600 text-white py-16">
        <div className="max-w-4xl mx-auto px-6 text-center">
          <div className="w-20 h-20 bg-white/20 rounded-full flex items-center justify-center mx-auto mb-6">
            <Heart className="w-12 h-12 animate-pulse" />
          </div>
          <h1 className="text-4xl lg:text-5xl font-bold mb-4">
            Support Our Mission
          </h1>
          <p className="text-xl text-blue-50 max-w-2xl mx-auto">
            Your generosity helps us maintain and expand this vital resource for Florida's neurodivergent community. 
            Every contribution makes a real difference.
          </p>
        </div>
      </div>

      <div className="max-w-6xl mx-auto px-6 -mt-8">
        {/* Impact Stats */}
        <div className="grid md:grid-cols-4 gap-4 mb-12">
          <Card className="border-none shadow-xl bg-white">
            <CardContent className="p-6 text-center">
              <Database className="w-8 h-8 text-blue-600 mx-auto mb-3" />
              <p className="text-3xl font-bold text-gray-900">500+</p>
              <p className="text-sm text-gray-600">Resources Listed</p>
            </CardContent>
          </Card>
          
          <Card className="border-none shadow-xl bg-white">
            <CardContent className="p-6 text-center">
              <Users className="w-8 h-8 text-purple-600 mx-auto mb-3" />
              <p className="text-3xl font-bold text-gray-900">10K+</p>
              <p className="text-sm text-gray-600">Families Helped</p>
            </CardContent>
          </Card>
          
          <Card className="border-none shadow-xl bg-white">
            <CardContent className="p-6 text-center">
              <TrendingUp className="w-8 h-8 text-green-600 mx-auto mb-3" />
              <p className="text-3xl font-bold text-gray-900">100%</p>
              <p className="text-sm text-gray-600">Free Access</p>
            </CardContent>
          </Card>
          
          <Card className="border-none shadow-xl bg-white">
            <CardContent className="p-6 text-center">
              <CheckCircle className="w-8 h-8 text-orange-600 mx-auto mb-3" />
              <p className="text-3xl font-bold text-gray-900">Weekly</p>
              <p className="text-sm text-gray-600">Updates</p>
            </CardContent>
          </Card>
        </div>

        <div className="grid lg:grid-cols-3 gap-8">
          {/* Donation Form */}
          <div className="lg:col-span-2">
            <Card className="border-none shadow-xl">
              <CardHeader className="border-b">
                <CardTitle className="text-2xl">Make Your Donation</CardTitle>
              </CardHeader>
              <CardContent className="p-6 space-y-6">
                {/* Donation Type */}
                <div>
                  <Label className="text-base font-semibold mb-3 block">
                    Donation Type
                  </Label>
                  <RadioGroup value={donationType} onValueChange={setDonationType}>
                    <div className="grid md:grid-cols-2 gap-4">
                      <Card className={`cursor-pointer border-2 transition-all ${
                        donationType === "one-time" ? "border-blue-500 bg-blue-50" : "border-gray-200"
                      }`}>
                        <CardContent 
                          className="p-4 flex items-center gap-3"
                          onClick={() => setDonationType("one-time")}
                        >
                          <RadioGroupItem value="one-time" id="one-time" />
                          <div>
                            <Label htmlFor="one-time" className="font-semibold cursor-pointer">
                              One-Time
                            </Label>
                            <p className="text-xs text-gray-600">Single donation</p>
                          </div>
                        </CardContent>
                      </Card>
                      
                      <Card className={`cursor-pointer border-2 transition-all ${
                        donationType === "monthly" ? "border-blue-500 bg-blue-50" : "border-gray-200"
                      }`}>
                        <CardContent 
                          className="p-4 flex items-center gap-3"
                          onClick={() => setDonationType("monthly")}
                        >
                          <RadioGroupItem value="monthly" id="monthly" />
                          <div>
                            <Label htmlFor="monthly" className="font-semibold cursor-pointer">
                              Monthly
                            </Label>
                            <p className="text-xs text-gray-600">Recurring support</p>
                          </div>
                        </CardContent>
                      </Card>
                    </div>
                  </RadioGroup>
                </div>

                {/* Amount Selection */}
                <div>
                  <Label className="text-base font-semibold mb-3 block">
                    Select Amount
                  </Label>
                  <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
                    {donationAmounts.map(({ amount: amt, impact }) => (
                      <Card 
                        key={amt}
                        className={`cursor-pointer border-2 transition-all ${
                          amount === amt.toString() && !customAmount 
                            ? "border-blue-500 bg-blue-50" 
                            : "border-gray-200 hover:border-blue-300"
                        }`}
                        onClick={() => {
                          setAmount(amt.toString());
                          setCustomAmount("");
                        }}
                      >
                        <CardContent className="p-4 text-center">
                          <p className="text-2xl font-bold text-gray-900 mb-1">
                            ${amt}
                          </p>
                          <p className="text-xs text-gray-600">{impact}</p>
                        </CardContent>
                      </Card>
                    ))}
                  </div>
                </div>

                {/* Custom Amount */}
                <div>
                  <Label htmlFor="custom-amount" className="text-base font-semibold mb-2 block">
                    Or Enter Custom Amount
                  </Label>
                  <div className="relative">
                    <DollarSign className="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                    <Input
                      id="custom-amount"
                      type="number"
                      placeholder="Enter amount"
                      value={customAmount}
                      onChange={(e) => setCustomAmount(e.target.value)}
                      className="pl-10 text-lg"
                      min="1"
                    />
                  </div>
                </div>

                {/* Donor Information */}
                <div className="space-y-4 pt-4 border-t">
                  <div>
                    <Label htmlFor="name">Full Name</Label>
                    <Input
                      id="name"
                      value={donorInfo.name}
                      onChange={(e) => setDonorInfo({ ...donorInfo, name: e.target.value })}
                      placeholder="John Doe"
                    />
                  </div>

                  <div>
                    <Label htmlFor="email">Email Address</Label>
                    <Input
                      id="email"
                      type="email"
                      value={donorInfo.email}
                      onChange={(e) => setDonorInfo({ ...donorInfo, email: e.target.value })}
                      placeholder="john@example.com"
                    />
                  </div>

                  <div>
                    <Label htmlFor="message">Message (Optional)</Label>
                    <Textarea
                      id="message"
                      value={donorInfo.message}
                      onChange={(e) => setDonorInfo({ ...donorInfo, message: e.target.value })}
                      placeholder="Share why you're supporting our mission..."
                      rows={3}
                    />
                  </div>
                </div>

                {/* Total and Submit */}
                <div className="pt-6 border-t">
                  <div className="flex items-center justify-between mb-4 p-4 bg-blue-50 rounded-lg">
                    <span className="text-lg font-semibold text-gray-900">
                      {donationType === "monthly" ? "Monthly" : "One-Time"} Donation:
                    </span>
                    <span className="text-3xl font-bold text-blue-600">
                      ${selectedAmount || "0"}
                    </span>
                  </div>

                  <Button 
                    size="lg" 
                    className="w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-lg py-6"
                    disabled={!selectedAmount || parseFloat(selectedAmount) < 1}
                  >
                    <Heart className="w-5 h-5 mr-2" />
                    Donate ${selectedAmount || "0"} {donationType === "monthly" ? "Monthly" : "Now"}
                  </Button>

                  <p className="text-xs text-gray-500 text-center mt-3">
                    Secure payment processing • Tax-deductible donation
                  </p>
                </div>
              </CardContent>
            </Card>
          </div>

          {/* Sidebar - Why Donate */}
          <div className="space-y-6">
            <Card className="border-none shadow-xl bg-gradient-to-br from-blue-600 to-purple-600 text-white">
              <CardContent className="p-6">
                <h3 className="text-xl font-bold mb-4">Your Impact</h3>
                <div className="space-y-4">
                  <div className="flex items-start gap-3">
                    <CheckCircle className="w-5 h-5 flex-shrink-0 mt-1" />
                    <div>
                      <p className="font-semibold mb-1">Accurate Information</p>
                      <p className="text-sm text-blue-100">
                        Keep resources verified and up-to-date
                      </p>
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <CheckCircle className="w-5 h-5 flex-shrink-0 mt-1" />
                    <div>
                      <p className="font-semibold mb-1">Expanded Coverage</p>
                      <p className="text-sm text-blue-100">
                        Add more resources across Florida
                      </p>
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <CheckCircle className="w-5 h-5 flex-shrink-0 mt-1" />
                    <div>
                      <p className="font-semibold mb-1">New Features</p>
                      <p className="text-sm text-blue-100">
                        Develop tools families need most
                      </p>
                    </div>
                  </div>
                  <div className="flex items-start gap-3">
                    <CheckCircle className="w-5 h-5 flex-shrink-0 mt-1" />
                    <div>
                      <p className="font-semibold mb-1">Free Access</p>
                      <p className="text-sm text-blue-100">
                        Keep the platform free for all families
                      </p>
                    </div>
                  </div>
                </div>
              </CardContent>
            </Card>

            <Card className="border-none shadow-xl">
              <CardContent className="p-6">
                <h3 className="text-lg font-bold text-gray-900 mb-4">
                  Other Ways to Give
                </h3>
                <div className="space-y-3">
                  <Button variant="outline" className="w-full justify-start">
                    <CreditCard className="w-4 h-4 mr-2" />
                    PayPal
                  </Button>
                  <Button variant="outline" className="w-full justify-start">
                    <Smartphone className="w-4 h-4 mr-2" />
                    Venmo
                  </Button>
                  <Button variant="outline" className="w-full justify-start">
                    <Mail className="w-4 h-4 mr-2" />
                    Check by Mail
                  </Button>
                </div>
              </CardContent>
            </Card>

            <Card className="border-none shadow-xl bg-green-50">
              <CardContent className="p-6">
                <p className="text-sm text-gray-700">
                  <strong>100%</strong> of your donation goes directly to maintaining and improving 
                  the Florida ASD Directory. We're committed to transparency and efficiency.
                </p>
              </CardContent>
            </Card>
          </div>
        </div>
      </div>
    </div>
  );
}