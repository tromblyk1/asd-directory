{
  "name": "asd-directory-deploy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SFTP deploy script for syncing src/frontend/dist/ to Hostinger web root",
  "scripts": {
    "deploy": "node deploy.mjs",
    "probe": "node probe.mjs",
    "clean": "node clean.mjs",
    "clean:force": "node clean.mjs --delete"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ssh2-sftp-client": "^10.0.3"
  }
}
